import { createContext } from 'react';

export const ArrangeTodosContext = createContext(null);

export const CreateTodoContext = createContext(null);

export const GetTodosContext = createContext(null);

export const SearchTodosContext = createContext(null);

export const RefresherContext = createContext(null);
